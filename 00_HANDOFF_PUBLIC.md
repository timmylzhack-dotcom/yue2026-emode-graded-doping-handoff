# Yue 2026 增强型分级掺杂 β-Ga₂O₃ MOSFET 公开交接

这是用于 Codex 与网页端 ChatGPT 协作的脱敏交接文档。精确的本机路径、远端路径、SSH 主机、账号和认证信息仅保存在本地私有交接文档中，不得补入公开仓库。

## 任务边界

- 不从零重新建模，现有 DevEdit、BetaGa2O3、BV 和 SEB/瞬态模板是母版。
- 优先读取英文论文 Markdown 及其中已经整理的笔记；只按需打开对应图片，不读取论文 PDF，不全盘搜索。
- 不生成或记录 SHA-256 哈希值。
- 材料身份必须是 `BetaGa2O3`，不能写成普通 Ga2O3，更不能误用 GaN。
- 不在栅边缘、场板边缘、凹槽拐角等电场边缘敏感区域新增局部网格加密。
- 当前阶段只建立接近论文结构和参数的首版基线，不做参数扫描，也不为拟合擅自拉偏材料或物理参数。
- 正式运行目录只保留 `.in`、`.str`、`.log`、`.typescript`。

## 论文器件结构

器件是横向顶栅凹栅增强型 MOSFET。半导体自下而上为 Fe 掺杂半绝缘 (010) β-Ga₂O₃ 衬底、100 nm UID β-Ga₂O₃、100 nm 且施主浓度为 `1e18 cm^-3` 的 n 型 β-Ga₂O₃，以及30 nm 且施主浓度为 `7e18 cm^-3` 的 n+ β-Ga₂O₃。

最终结构中，源漏接触下保留30 nm n+ 层；源漏之间移除 n+ 帽层；栅下进一步完全移除100 nm n 层，使栅介质直接位于 UID 层上。栅介质为20 nm Al₂O₃，钝化层为300 nm SiO₂。Gate FP 与栅极连接，Source FP 与源极连接，两者不能误并成同一个偏置电极。Source FP 位于 SiO₂ 上方并向漏端延伸3.5 µm。

横向尺寸采用 `LGS=1.5 µm`、`LG=2.0 µm`、`LGD=10.0 µm`、`LSD=13.5 µm`。当前坐标约定为：源极右边界 `x=1.0 µm`，底部栅脚 `x=2.5–4.5 µm`，漏极左边界 `x=14.5 µm`。

栅极顶部栅头/场板采用 `x=2.0–5.0 µm`，总宽3.0 µm。相对于 `x=2.5–4.5 µm` 的2.0 µm 栅脚，这组明确坐标对应左右各外伸0.5 µm；它不等于左右各0.2 µm。如果物理目标确实是各外伸0.2 µm，则顶部应改为 `x=2.3–4.7 µm`，所以当前以明确的 `2.0–5.0 µm` 为准。

所有电极必须建成有面积和厚度的金属区域，不能用零厚度线电极代替。源极和漏极采用 Ti/Au=`60/180 nm`；栅极以及与栅相连的场板采用 Ni/Au=`50/150 nm`。不同金属层可使用相同电极编号实现电气连接，但几何区域和材料层次应保留。

## 二维宽度约定

论文小周长器件的总沟道宽度为 `Wch=20 µm`，它是当前直流和高压主拟合对象，因此二维首版基线使用 `MESH WIDTH=20`。

论文大周长器件的总栅宽为 `20 mm=20000 µm`，属于梳齿形多指器件。如果单根栅指宽度取500 µm，则 `MESH WIDTH=500` 仅代表一根栅指，理想几何倍数为 `20000/500=40`。仅把二维 `WIDTH` 放大到500或20000，只会线性缩放理想电流，不能自动产生多指互连压降、电流拥挤、缺陷统计和三维热扩散，也不能据此强行拟合大器件约620 V 的击穿结果。

## 当前进度

首轮 DevEdit/ATLAS 基线已经完成材料载入、结构生成、初始化以及低漏压工作点，正式日志的最后实际偏压为 `VG=0 V`、`VD=0.1 V`。结构文件中材料身份为 `BetaGa2O3`，初始化和两个低漏压点均收敛。

首轮结构仍是检查基线，不应作为最终论文几何。下一版的最小修改是：把顶部栅头从旧范围改为 `x=2.0–5.0 µm`；把源、漏、栅和场板全部改成实际厚度的 Ti/Au 或 Ni/Au 区域；小周长版本明确采用 `MESH WIDTH=20`。保持既有 BetaGa2O3 材料和物理模型，不同时进行参数拟合。

## 请网页端复核

请只依据本公开交接和论文信息回答以下问题，不建议从零重写器件：

1. `x=2.0–5.0 µm` 的3 µm 栅头相对于 `x=2.5–4.5 µm` 的栅脚是否应明确记录为左右各0.5 µm 外伸？
2. 小周长二维主基线采用 `MESH WIDTH=20` 是否正确？若大器件单指宽度为500 µm，是否应把 `WIDTH=500` 明确解释为单指而非20 mm 整器件？
3. 在 DevEdit 中把 Ti/Au 和 Ni/Au 分层建成有厚度区域、并让同一端的多个区域共享电极编号，是否是比零厚度线电极更合适的首版表达？
4. 在不新增电场边缘局部网格加密、不拉偏物理参数的前提下，下一步最小结构检查项目还缺什么？

网页端反馈应明确区分“论文直接给定”“由坐标计算”“二维近似”和“仍需用户确认”，不要把推断写成论文原值。
