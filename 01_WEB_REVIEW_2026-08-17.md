# 网页端复核记录（2026-08-17）

复核入口：[公开交接文档](https://github.com/timmylzhack-dotcom/yue2026-emode-graded-doping-handoff/blob/main/00_HANDOFF_PUBLIC.md)

网页端对话：[Silvaco 仿真拟合条件](https://chatgpt.com/s/t_6a8307f3da448191afec9072f4184b51)

本记录只保存公开脱敏信息。网页端已读取 GitHub 渲染页及原始 Markdown，并按要求只复核公开交接中的四个问题。

## 已确认

- 栅脚 `x=2.5–4.5 µm`，宽度2.0 µm；栅头 `x=2.0–5.0 µm`，宽度3.0 µm。由坐标计算，左、右外伸均为0.5 µm，不能写成0.2 µm。
- 上述坐标与 `LGS=1.5 µm`、`LG=2.0 µm`、`LGD=10.0 µm`、`LSD=13.5 µm` 自洽。这里属于坐标一致性检查，不是额外的论文独立参数。
- 小周长主拟合器件的 `Wch=20 µm`，二维首版基线使用 `MESH WIDTH=20`。
- 如果另做 `MESH WIDTH=500`，必须标注为“500 µm 单指二维近似”，不能称为20 mm 多指整器件。`20000/500=40` 只是理想宽度倍数，不能代替多指互连、电流拥挤、缺陷统计和三维热模型。
- DevEdit 首版应把 Ti/Au=`60/180 nm` 和 Ni/Au=`50/150 nm` 建成有面积、有厚度的分层金属区域。属于同一偏置端的多个金属区域可以共享电极编号，但仍保留各自的几何和材料层次。
- Gate FP 与 Gate 可以同电位，Source FP 与 Source 可以同电位；Gate 与 Source/Source FP 不能误短接。

## 下一版最小检查

- 检查所有横向坐标和层间界面闭合。
- 检查30 nm n+ 只在源漏接触下保留，源漏之间 n+ 已移除，栅下100 nm n 层已完全移除，20 nm Al₂O₃直接接触 UID 层。
- 检查300 nm SiO₂、金属层厚度、电极编号和实际连通关系。
- 检查网格连续、无退化单元，并确认结构仍可初始化、低漏压仍可收敛。
- 保持“不在电场边缘敏感区域新增局部网格加密”的冻结约束，不同时调整物理参数。

## 仍需用户或论文图确认

- `500 µm` 是否确实是大周长器件的单根实际栅指宽度；目前只能作为待确认假设，不能标成论文已直接给定。
- Source FP 的精确起止坐标、垂直位置、覆盖关系以及与 Source 的过孔/金属连接实现。公开交接目前只冻结了其位于 SiO₂ 上方并向漏端延伸3.5 µm。

因此下一版可以先冻结 `Gate foot=2.5–4.5 µm`、`Gate head=2.0–5.0 µm`、`MESH WIDTH=20` 和实际厚度金属区域，完成结构、层次、连通性及收敛检查；不要把未确认的500 µm单指或 Source FP 细部写成论文确定值。
