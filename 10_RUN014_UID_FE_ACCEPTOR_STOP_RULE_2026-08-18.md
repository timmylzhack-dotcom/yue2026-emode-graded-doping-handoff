# RUN014：UID 动态深受主路线的停止判据（2026-08-18）

任务 session ID：`01a00f0f-a888-78f3-b717-e81e4367a0c7`

RUN014 是 RUN013 的单因素继承测试。唯一物理变化是把 ATLAS region 5（100 nm UID channel）的受主型深能级密度从 `1.0e18` 降为 `1.0e17 cm^-3`；能级 `0.8 eV`、俘获截面 `sign=sigp=5e-15 cm^2`、几何、网格、BetaGa2O3 材料、迁移率、gate WF=`5.78 eV`、`Qf=0`、`Dit=0`、衬底陷阱及求解路径全部不变。该密度仍是诊断试探值，不是论文值。

远端运行目录为 `/root/YUE2026_EMODE_GRADED_DOPING/RUN014_UID_FE_ACCEPTOR_1E17/`。tmux 会话 `yue_run014_uidfe1e17_20260818` 已正常结束；ATLAS 实际完成到 `VGS=12 V`，121 个扫描点完整，无 ATLAS 错误，最大绝对 KCL 为 `1.00e-15 A`。

| 指标 | RUN012（无 UID 深受主） | RUN014（UID 深受主 1e17） |
|---|---:|---:|
| `ID @ VGS=3.1 V` | `2.0081e-5 A` | `2.7623e-6 A` |
| `ID @ VGS=9 V` | `2.3119e-5 A` | `1.0202e-5 A` |
| `ID @ VGS=12 V` | `2.3471e-5 A` | `1.1592e-5 A` |
| `ID @ 12 V`，20 μm 归一化 | `1.1735 mA/mm` | `0.5796 mA/mm` |
| `VTH @ 1 mA/mm` | `3.0644 V` | 0–12 V 内无交点 |
| 1 μA 低电流交点 | `1.9070 V` | `2.7650 V` |
| 2 μA 低电流交点 | `1.9534 V` | `2.9709 V` |

RUN014 证明 `1e17 cm^-3` 的动态深受主确实能把低电流交点正移约 `0.9–1.0 V`，但代价是 12 V 电流下降约 `50.6%` 并形成明显的高栅压平台。窄低电流窗口 `1e-12..1e-8 A` 的线性拟合 SS 约为 `85 mV/dec`，但扩展到 `1e-10..1e-6 A` 后表观 SS 约为 `283 mV/dec`；该窗口依赖说明曲线不是理想刚性平移，不能把单一 SS 数值冒充正式拟合结果。

网页端事先给出的停止条件是：若 `ID@12 V < 2.0e-5 A`，或阈值移动效率低而高栅压电流下降超过约 20%–30%，就停止用动态深受主粗调阈值。RUN014 同时触发两项，因此禁止继续提高该动态陷阱密度，也不在 `1e17..1e18` 之间继续扫描。下一物理路线应改为等效静态 UID 净补偿，保持动态陷阱关闭；网页端建议的首个诊断量级是 `5e17 cm^-3`，但必须标为静电代理/试探值，不能称为论文给出的真实受主掺杂。

