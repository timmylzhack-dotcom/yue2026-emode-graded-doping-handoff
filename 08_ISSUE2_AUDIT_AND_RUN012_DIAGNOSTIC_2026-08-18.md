# Issue 2 审核与 RUN012 中间偏压诊断（2026-08-18）

Codex 任务 session ID：`01a00f0f-a888-78f3-b717-e81e4367a0c7`

## 网页端审核结论

网页端结合全器件/栅区网格、Id-Vg 曲线、Electron Concentration、Potential、RUN011 代码和正式 KCL 后判断：网格或区域错误作为 `VTH≈3.06 V` 主因的可能性低。现有图像显示 VGS=12 V 时电子积累沿整个栅底连续形成，而不是沿角点、侧壁细缝或单列三角形泄漏；Potential 也没有网格方向相关的棋盘振荡或孤立通道。

RUN011 当前实际代表的是：UID 浅施主 `5e15 cm^-3`、继承的 5.78 eV 有效功函数、`Qf=0`、`Dit=0`。后三类栅控电静力学参数及 UID 净补偿均不是论文直接给出的值。

网页端建议的物理校准顺序为：UID 净补偿 → 有效功函数 → 固定界面电荷 → 界面态/体陷阱。不能同时无约束调节这些参数；功函数不能单独承担约 5.9 V 的全部阈值偏移；Dit 应在阈值位置基本确定后用于匹配 SS 和迟滞。

## RUN012 不改物理诊断

RUN012 完全继承 RUN011 的几何、网格、BetaGa2O3 材料、掺杂、迁移率、Fe 衬底陷阱、功函数和求解方法，只增加 VGS=3.1 V 与 9.0 V 的 STR 保存和 Conduction Band 输出。

- VGS=3.1 V：`ID=2.008e-5 A`，约 `1.004 mA/mm`；绝对 KCL 约 `5.94e-17 A`；第二次 Newton 迭代电子 RHS 约 `10^-13.79`。
- VGS=9.0 V：`ID=2.312e-5 A`，约 `1.156 mA/mm`；绝对 KCL 约 `7.12e-18 A`；第二次 Newton 迭代电子 RHS 约 `10^-11.54`。
- VGS=12 V：仍得到 RUN011 相同的约 `1.1735 mA/mm`；没有新语法错误或 ATLAS 警告。

上述结果不支持 X-norm 假收敛、端子短接或数值漏电产生 20 uA 电流。3.1 V 与 9 V 的正式结构分别是：

- `/root/YUE2026_EMODE_GRADED_DOPING/RUN012_VG3P1_VG9_DIAGNOSTIC/YUE2026_RUN012_VG3P1_VG9_DIAGNOSTIC_vd1_vg3p1.str`
- `/root/YUE2026_EMODE_GRADED_DOPING/RUN012_VG3P1_VG9_DIAGNOSTIC/YUE2026_RUN012_VG3P1_VG9_DIAGNOSTIC_vd1_vg9.str`

## 阈值修复边界

下一次允许的首个单因素测试应针对 UID 净补偿，但不能把没有来源的浅受主浓度写成论文值。仅把 `5e15 cm^-3` 浅施主降一点，预计不足以产生约 6 V 的位移；若使用净受主补偿或受主型深能级，必须说明其物理来源、占据状态以及是否会引入迟滞/扫速依赖。

固定负界面电荷可以使 n 沟道 VTH 正移，但单独产生约 6 V 位移需要约 `1e13 cm^-2` 量级，只能作为数量级诊断，不能直接当作论文值。有效功函数只允许在可信 Ni/Au 范围内小幅校准；界面态/体陷阱最后用于匹配论文约 105 mV/dec 的 SS 和约 0.1 V 的小器件迟滞。

另一个必须并行保留的约束是高栅压电流：RUN012 在 VGS=9..12 V 仅由约 1.156 增至 1.174 mA/mm，论文的 1 mA/mm 阈值判据已经接近当前电流平台。因此任何阈值右移方案都必须同时检查 VDS=1 V 的高栅压电流、后续 Rsh/Ron 目标，不能只看常电流交点。
