# Codex 筛选后的上层结构修改备忘录（2026-08-17）

本备忘录以论文英文 Markdown、当前公开 IN 和网页端复核意见为依据。网页端意见不是直接执行清单；与论文原文、用户冻结约束或几何闭合性冲突的部分已经剔除。

## 论文直接给出的事实

- 器件材料为 β-Ga₂O₃；当前仿真中的材料身份继续保持 `BetaGa2O3`。
- Source/Drain 为 Ti/Au=`60/180 nm`。
- Gate 和 Gate FP 为 Ni/Au=`50/150 nm`。
- Gate 形成后沉积 `300 nm` PECVD SiO₂；随后 RIE 开孔，再形成 Ti/Au=`60/180 nm` 的 Source FP。
- Source FP 向漏极方向延伸 `3.5 µm`。
- 论文 Fig. 1(d) 正文明确写有 `500 µm finger width` 和 `20 mm gate width`。因此 500 µm 单指宽度应标为论文值，不是待确认假设；但它仍不等于在二维模型中直接复现完整 20 mm 多指器件。
- 小器件为 `Wch=20 µm`。ATLAS 二维模型采用 `MESH WIDTH=20`，使电流按 20 µm 器件宽度缩放；这只处理二维宽度，不包含多指互连、电流拥挤或三维热效应。

## 可以直接执行的最小结构修改

保留现有 DevEdit 半导体层、分级掺杂、BetaGa2O3 材料参数和 ATLAS 物理模型，只重建顶层介质与金属。Gate foot 固定为 `x=2.5–4.5 µm`，Gate head/Gate FP 固定为 `x=2.0–5.0 µm`；后者总宽 3.0 µm，相对 foot 左右各伸出 0.5 µm。当前代码中的 `x=2.5–5.5 µm` 必须改掉。

Source 和 Drain 分别拆成有面积的 Ti、Au 两层：Ti 从接触表面向上 0.06 µm，Au 再向上 0.18 µm。两层共享各自端子的 `elec.id`。Gate/Gate FP 也必须拆成有面积的 Ni、Au 两层，总厚度分别为 0.05 µm 和 0.15 µm，并共同使用 Gate 的 `elec.id=3`。

Gate/Gate FP 先形成，SiO₂ 后沉积，所以 SiO₂ 必须位于 Gate/Gate FP 上方并把它与最后形成的 Source FP 隔开。Source FP 必须是位于 SiO₂ 外表面的有面积 Ti/Au 叠层，并通过 Source 一侧的开孔/竖向金属连接到 `elec.id=1`。Source FP 和 Gate/Gate FP 之间不能出现共享金属区域或相同电极编号。

`region id=12`、`id=14`、`id=15` 目前只有两个点，是零面积线区域，应删除并用闭合 polygon/rectangle 替代。当前单一 SiO₂ polygon 也应删除，改成互不重叠的分段区域：访问区上方、Gate/Gate FP 上方以及过孔两侧分别建区，以避开已有金属实体。`work.area` 的上边界应从 `y=-0.31 µm` 上移到约 `y=-0.80 µm`，为 300 nm SiO₂、240 nm Source FP 和台阶留出余量；这个 `-0.80 µm` 是建模余量，不是论文尺寸。

## 不能直接照抄网页端的部分

网页端曾建议把 Gate Au 简化成 `x=2.0–5.0、y=-0.09–0.06 µm` 的整块矩形。该矩形在 `x=2.0–2.5` 和 `x=4.5–5.0 µm` 区间会向下穿入访问区 Al₂O₃/半导体，几何上不成立，不能直接执行。Gate 金属应改成不侵入访问区的阶梯/T 形 polygon，或者拆成互不重叠的 central stem 与左右 overhang。论文没有披露 Ni/Au 在这个 T 形截面中的精确轮廓，因此最终转折坐标必须标为“二维工程近似”，先通过结构闭合和电极连通检查，再由结构图人工确认。

网页端给出的 Source 过孔 `x=0.4–0.8 µm` 没有论文依据，也不能冻结为论文值。过孔只需先放在 Source 金属上方、避开 Source 边缘和 Gate 方向的高场敏感位置，并明确标注为二维近似。Source FP 的论文直接参数只有 Ti/Au 厚度、位于 SiO₂之后形成，以及向漏端延伸 3.5 µm；“从 Source 右边界 `x=1.0 µm` 起算到 `x=4.5 µm`”是当前首版的坐标映射，不是论文原句。

候选拟合指标 Markdown 中要求在栅边缘、场板边缘和凹槽拐角局部加密的内容与用户冻结约束冲突，本项目不执行。正式运行目录增加 CSV、TXT 或额外 Markdown 的建议也不执行；正式目录仍只保留 `.in`、`.str`、`.log`、`.typescript`。

## 当前 IN 的其他审查结果

`user.default=GaN` 是继承模板留下的潜在风险，但当前半导体区使用 UD1/BetaGa2O3，且关键材料参数已显式设置；在没有证据表明它改变已生成 STR 材料身份前，不应为了清理文字而顺带大改物理模型。下一轮应继续用 STR/材料区检查确认 BetaGa2O3 身份。

当前低压 R0 只验证结构载入、初始化和低漏压收敛，尚未包含完整 BV 扫描不是错误。顶层结构修正后的 RUN002 也应先做同样的低压检查：DevEdit 成功生成 STR，区域无重叠/退化，Source、Gate、Drain 电极编号正确，Source FP 与 Gate FP 不短接，ATLAS 能载入并初始化。通过后再进入转移或耐压流程，不在这一轮调整 UID、功函数、界面电荷、陷阱或冲击电离参数。
